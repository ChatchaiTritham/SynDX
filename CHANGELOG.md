# Changelog

## Unreleased

- stabilize package imports and add lightweight compatibility modules
- add package smoke coverage and CLI entry point validation
- align `pyproject.toml` metadata with package dependencies and scripts
